import React from 'react';
export declare const ErrorPopup: React.FC<ErrorPopupProps>;
interface ErrorPopupProps {
    showErrorPopup?: boolean;
    setShowErrorPopup: (status: boolean) => void;
}
export {};
